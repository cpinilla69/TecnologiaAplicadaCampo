# Aplicación de Reservas para Comunidad

## Instalación

```bash
npm install
```

## Uso

```bash
npm start
```

La aplicación se ejecutará en `http://localhost:3000` y usará el directorio `public` como frontend. Todas las reservas se guardan en `server/reservas.txt`.
